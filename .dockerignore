# Agent state is a runtime volume, never image content. Copying a local
# data/ into the image would ship one machine's projects, sent messages and
# approvals to the deployment, and mask an unset ASHRAH_DATA_DIR.
data/
.env

.git/
.github/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
